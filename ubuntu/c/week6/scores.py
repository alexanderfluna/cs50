# Creates an array called scores that's appended, adding values to the array
scores = []
scores.append(72)
scores.append(73)
scores.append(33)
print(f"Average: {sum(scores) / len(scores)}")

# Initially creates a set array with three values
scores = [72, 73, 33]
print(f"Average: {sum(scores) / len(scores)}")

